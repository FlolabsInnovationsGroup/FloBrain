{
  "name": "authentication-strategy",
  "version": "1.0.0",
  "description": "CAIPO authentication service (JWT-based)",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "generate-token": "node -e 'process.env.DOTENV_DISABLE_LOGS=\"true\"; require(\"dotenv\").config(); const jwt=require(\"jsonwebtoken\"); process.stdout.write(jwt.sign({sub:\"user-123\", roles:[\"admin\"]}, process.env.JWT_SECRET, {expiresIn: process.env.JWT_EXPIRES_IN || \"1h\"}));'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "author": "Filip Lysak",
  "license": "MIT"
}
