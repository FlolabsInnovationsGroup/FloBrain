const express=require("express"),cors=require("cors"),authRoutes=require("./api/auth/auth.routes"),userRoutes=require("./api/users/users.routes"),app=express();app.use(cors()),app.use(express.json()),app.use("/api/auth",authRoutes),app.use("/api/users",userRoutes),module.exports=app;