[mypy]
python_version = 3.11
# Treat untyped Django/DRF as valid (install django-stubs + djangorestframework-stubs for strict checking)
ignore_missing_imports = True
# Optional: type-check migrations
exclude = (migrations/|\.venv)
